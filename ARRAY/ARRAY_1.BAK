#include<stdio.h>
#include<conio.h>
void main(){
	  int arr[5]={10,20,30,20,40},i;
	  int num,count=0;
	  clrscr();

	  printf("enter a number to search: ");
	  scanf("%d",&num);

	  for(i=0;i<5;i++){
	    if(arr[i]==num){
	      count++;
	    }
	  }
	  if(count>0){
	    printf("the number %d appears %d time in the array.\n",num,count);
	  }
	  else{
	      printf("the number %d is not found in the array.\n",num);
	  }

	 getch();



}